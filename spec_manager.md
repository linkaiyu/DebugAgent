# description
spec_manager.py implement the operation to navigate the .spec.json file, for creating, updating, deleting the nodes when user provide new prompts.
it is called by AI Agent when user use /spec skill
